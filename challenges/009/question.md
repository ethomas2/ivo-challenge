# Question 9: Decryption

The binary file "ciphertext.txt" is encrypted using the key sha256("banana"). It uses AES-256 in Galois Counter Mode. The first 12 bytes are the IV, and the last 16 bytes are the auth tag. What is the final word in the file?
